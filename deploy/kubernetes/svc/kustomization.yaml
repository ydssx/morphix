configMapGenerator:
  - name: morphix-config
    files:
      - config.yaml
    namespace: morphix
resources:
  - gateway-svc.yaml
  - sms-svc.yaml
  - user-svc.yaml
  - payment-svc.yaml
  - order-svc.yaml
  - job-svc.yaml
generatorOptions:
  disableNameSuffixHash: true
namespace: morphix
commonLabels:
  project: morphix

# images:
#   - newTag: 1.0.2